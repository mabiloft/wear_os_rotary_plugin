name: wear_os_rotary_plugin
description: A Flutter package providing a circular scrollbar overlay for Wear OS screens that works seamlessly with the wearable_rotary package.
version: 0.1.0
homepage: https://github.com/mabiloft/wear_os_rotary_plugin
repository: https://github.com/mabiloft/wear_os_rotary_plugin
issue_tracker: https://github.com/mabiloft/wear_os_rotary_plugin/issues

environment:
  sdk: ^3.0.0
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  wearable_rotary: ^2.0.3

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0

